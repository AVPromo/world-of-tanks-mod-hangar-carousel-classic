# Hangar Carousel Classic 1.0.5

- Fix native vehicle filtering so active Classic filters are applied to the carousel data source.
- Show all Classic filters directly in the native vanilla carousel filter popover.
- Synchronize active filter buttons through the Gameface view model.
- Remove obsolete ModsSettingsAPI filter-visibility checkboxes.